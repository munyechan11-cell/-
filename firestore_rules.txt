rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // [권장 규칙] 
    // 현재 앱의 새로운 컬렉션 구조에 맞춘 보안 규칙입니다.
    // 임시로 모든 읽기/쓰기를 허용하며, 서비스가 안정화된 후 세부 권한(로그인한 사용자만 등)을 설정하는 것을 권장합니다.

    match /users/{userId} {
      allow read, write: if true;
    }
    
    match /visits/{visitId} {
      allow read, write: if true;
    }
    
    match /coupons/{couponId} {
      allow read, write: if true;
    }
    
    match /tables/{tableId} {
      allow read, write: if true;
    }
    
    match /communications/{commId} {
      allow read, write: if true;
    }
    
    match /tierOverrides/{overrideId} {
      allow read, write: if true;
    }
    
    match /appState/{docId} {
      allow read, write: if true;
    }
    
    // 하위 호환성 및 안전을 위한 포괄적 허용
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
